[project]
name = "ai-model-trainer-python-image-example"
version = "0.1.0"
description = "Small Python example that loads the AI Model Trainer Diffusers LoRA adapter."
requires-python = ">=3.10"
dependencies = []
